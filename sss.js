let arr = []
function promppt() {
    let str = prompt("Komanda kiriting (add, del yoki stop)")
    for (let i = 0; i < Infinity; i++) {
        if (str == "stop") {
            break
        }
    }
}
promppt();